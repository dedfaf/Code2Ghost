{
  "name": "code2ghost",
  "displayName": "Code2Ghost",
  "description": "A extension to publish local MD files to Ghost CMS",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.96.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "code2ghost.helloWorld",
        "title": "Hello World"
      },
      {
        "command": "code2ghost.getJwt",
        "title": "Get JWT"
      },
      {
        "command": "code2ghost.createPost",
        "title": "Create Post"
      }
    ]
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "npm run lint",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "20.x",
    "@types/vscode": "^1.96.0",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.4.1",
    "eslint": "^9.16.0"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "jsonwebtoken": "^9.0.2"
  }
}
